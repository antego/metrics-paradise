jetty {
  host = localhost
  port = 8080
}

zookeeper {
  connect.timeout.sec = 30
  root.node.name = /metrics-home
  node.prefix = node
  port = 2181
  host = localhost
}

advertise {
  host = localhost
  port = 8080
}

db {
  h2.url = "jdbc:h2:mem:"
  result.limit = 10000000
}